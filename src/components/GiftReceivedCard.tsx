import { h, FunctionComponent } from "preact";

const GiftReceivedCard: FunctionComponent = () => (
  <div className="card-front card-gift-received">
    <h3>Подарок получен</h3>
  </div>
);

export default GiftReceivedCard;
